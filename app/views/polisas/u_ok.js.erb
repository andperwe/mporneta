if (nowa_polisa == 0)
{
t2.ajax.reload( null, false );
$('#polisafd').dialog( "close" );
$( "#komunikat_p" ).dialog({
              autoOpen: false,
              maxWidth:450,
              maxHeight: 220,
              width: 450,
              height: 220,
              modal: true,
              buttons:[
                      {
                        id:"uok_kom_p",
                        text: "Ok",
                        click: function() {
                          $('#komunikat_p').dialog("close");
                          t2.ajax.reload( null, false );
                          t3.ajax.reload( null, false );
                          t4.ajax.reload (null, false);
                          t5.ajax.reload(null, false);
                        }
                      }
                    ]
});

$("#uok_kom_p").mouseover(function() {
   $('button').removeClass("ui-state-hover");
});
$("#uok_kom_p").focus(function () {
    $(this).removeClass("ui-state-focus");
});
$('#uok_kom_p').removeClass();
$('#uok_kom_p').addClass('btn btn-success btn-sm');

$('#komunikat_p').html("<%= escape_javascript (render partial: 'u_ok') %>");
$('#komunikat_p').dialog( "open" );
}
